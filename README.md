For import stands from attached csv
    
    1.npm install
    2.npm run import-stands


For run project
    
    1. npm install
    2. npm start


For running with docker 
    
    1. npm install
    2. npm run buid
    3. docker-compose up
